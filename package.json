{
  "name": "playwright-screen-recorder-api",
  "version": "1.0.0",
  "description": "API that records browser interactions using Playwright and FFmpeg",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx playwright install --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.43.1",
    "uuid": "^9.0.0"
  }
}
